version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    # Define o nome do contêiner para facilitar o gerenciamento
    container_name: ad_manager_web
